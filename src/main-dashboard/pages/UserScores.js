import React from 'react';
import PlaceholderPage from './PlaceholderPage';

const UserScores = () => (
  <PlaceholderPage
    title="User Scores"
    description="Manage routing scores for your users"
  />
);

export default UserScores;
